{
  "name": "the-workflow-engine",
  "private": true,
  "workspaces": ["apps/*", "libs/*"],
  "scripts": {
    "dev": "tilt up",
    "test": "bun run --filter '*' test",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "check:boundaries": "bun run scripts/check-boundaries.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "lefthook": "^1.11.0",
    "typescript": "^5.8.0"
  },
  "trustedDependencies": ["@biomejs/biome", "better-sqlite3", "esbuild", "lefthook", "protobufjs"]
}
